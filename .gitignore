*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Playwright
/test-results
/playwright-report
/playwright/.cache
/blob-report
/.playwright-mcp

# Scraper output (Faza 1) — poze + JSON mari, date locale, nu în repo
/storage/scrape/
scripts/scraper/node_modules

# Screenshot-uri de review (Faza 3) — nu în repo
/faza3-*.png

# Screenshot-uri de review (Faza 4a) — nu în repo
/faza4a-*.png

# Screenshot-uri hero + logo source brut (review) — nu în repo
/hero-*.png
/logo-check.png
/image.svg

# Prototipuri de design (referință vizuală de forme/feel) — nu în repo
/design/

# Screenshot-uri de review (Homepage v2) — nu în repo
/homepage-v2-*.png

# Sitemap generat (Part 4) — scris de sitemap:generate pe prod, nu în repo
/public/sitemap.xml

# Screenshot-uri de review (Faza 4b) — nu în repo
/faza4b-*.png
/.playwright-mcp/
